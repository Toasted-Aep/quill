# Quill — to-do

Created 2026-09-05 from ROADMAP.md's "Known risks, unowned", plus the two
screen checks left unreached when the machine auto-locked.

Ordered by **user-facing severity first, then by whether it can be fixed
without guessing**. Each item names what "done" means, because several of
these have a fix that is easy and a *verification* that is not.

Status key: `TODO` · `IN FLIGHT` · `NEEDS SCREEN` · `DONE` · `RULING NEEDED`

---

## Wave 1 — finish what is built but unseen  ·  DONE

| # | item | done means |
|---|---|---|
| 1.1 | **Flipped COPIC labels at a bottom dock** (`b8e3512`) — **DONE, PASS** (run 16): every label upright at both the 9 o’clock (`BottomRight` dock) and 3 o’clock (`BottomLeft` dock) seams, no double-flip. | every label upright including at the 9/3 o'clock seam; none flipping twice near the boundary |
| 1.2 | **`BottomMenu` pill under Theme = Light** (`b8e3512`) — **DONE, PASS** (run 16): resting pill `#78363C` matches the Red page’s panel (7.84:1, was 1.50:1); repaints immediately on a page turn with the menu already open, no relaunch. | resting pill shows the page's panel colour, not the pale gallery grey; repaints on a theme change *while a page is already open* |

## Wave 2 — chrome contrast: three faults, one class

**2.0 goes first: it protects the verification of everything behind it.**

| # | item | done means |
|---|---|---|
| **2.0** | **A second legacy-import path bypasses the scratch-isolation gate** — **DONE, FIXED** (run 17): reading `Load()` end to end there were **four** automatic out-of-folder reads, not one; all four are now gated on `IsIsolated`. Proven by three launches — an empty folder, run 16's exact leaking shape, and runs 7–11's shape — all ending with **0** of the user's notebook Ids present. The path closed, not the seeding widened. | a seeded scratch folder imports nothing, proven by a run that starts empty and stays empty |
| 2.1 | **Precision panel has no plate** — **DONE, FIXED** (run 17): it was excluded **deliberately**, by a measured reference (bare, no chrome), so the plate stays gone and the *marks* were re-keyed to the page instead — new `PageTheme.OnPage/OnPageMuted/PageOutline/PageIsDark`. On screen on `#FCFCFC`: headings and chips **1.091:1 → 17.957:1**, description **1.044:1 → 4.012:1**. The harness then failed the run on Blueprint/Brown Paper muted (2.16:1, pre-existing), so the muted alpha got a floor; worst over nine papers **2.164:1 → 3.006:1**. | it uses the same panel machinery Settings does |
| 2.2 | **Text editor ground 2.93:1** — **DONE, FIXED** (run 17): `#606060` was WinUI's `TextControlBackgroundFocused` (`#B31E1E1E`, dark theme) composited over the paper, because `RootGrid.RequestedTheme` is keyed to the **shell**. The box's own `Transparent` and its own ink are now pinned against WinUI's visual states, so the editor stands on the page. On screen **2.93:1 → 17.97:1**; worst of the nine papers **4.374:1**, and a 636,056-ground sweep puts the floor at **4.183:1 over the whole gamut**. §0 forced the mark to move with the ground: fixing the ground alone would have shipped `#FFFFFF` on `#FCFCFC` = **1.02:1** on a re-opened box. | clears the 3:1 floor on all nine shipped papers, measured by a harness |
| 2.3 | **Dial seat on Plain White 1.020:1** — **DONE, FIXED** (`9a1f294`, §41): `PagePlate.SeatBackdrop` composites the page through the shadow’s own alpha, read from `ShadowBrush` rather than guessed. Every seat lies wholly inside the gradient’s SOLID part (band 59.14..85.14 DIP, centre offset 2 DIP), so a seat sees a flat backdrop and one figure is honest. Floor value unchanged; only the surface it is measured against moved. Disc byte-identical over 636,056 grounds. §24/§29’s light-branch figures corrected in place. Not verified on screen. Original: — it stands on the dial's **drop shadow**, not the page, so every figure §24/§29 computed for that case used the wrong surface | the plate formula knows the shadow exists, or the seat is lifted off it |

## Wave 3 — text colour: two defects the sweep found

| # | item | done means |
|---|---|---|
| 3.1 | **Format-bar per-run picker broken** — **NOT FOUND** (run 19, §40.3): all four symptoms measured and all four absent. The box does not whiten on open (box B, whose field and RTF disagree, is a live detector and did not move); `#9236FF` picked from the spectrum landed on all 3003 glyph-core pixels; it survives blur and re-open; and the file afterwards reads `TextColor: null` plus the run's own colour — **both halves of §25.3 hold**. What *was* wrong: the picker **never reported its subject**, opening at WinUI's `#FFFFFF` and thereafter at some other box's colour. **FIXED** — `TextColorFlyout_Opening` syncs it from the same range the setter writes, guarded so the sync cannot be mistaken for a pick; seen on screen opening at `#C2185B` over the crimson box. The one real symptom behind the report is 3.3's, not this row's — see §40.5. | §25.3's *last-control-wins* holds in both directions |
| 3.2 | **Coloured box shows white in the editor from its second open onward** — **NOT FOUND** (run 19, §40.1–40.2). Measured on a seeded box, on a box whose RTF disagrees with its field, on a live-created box that took its colour from `DefaultTextColor` without any colour control being driven, and across an app restart: `#C2185B` on every open, pixel counts identical. It was real when run 15 saw it — it is **item 2.2's defect** (`TextControlForegroundFocused` = the dark theme's `#FFFFFF` on a *re-opened* box), which §33/§2.2 diagnosed and `PinEditorBrushes` closed. Box C is the control: an uncoloured box now opens `#141413`. Nothing was built for this row. | second and later opens show the box's own colour |
| 3.3 | **Per-run colour still flattens on export** — **PARTIAL** (runs 20–21, §43–§44): the code is complete and measured everywhere a console can measure it; **one link is unseen.** §40.5's precondition is closed first (`1509498`): `box.Foreground` flattens the document when the template applies, so `BuildTextUi` captures what it was built from and restores it on `Loaded`, guarded so it fires only on a real loss, only once, and only while the characters are untouched. Then the four emitters (`de8f84c`): PDF `rg`/`RG` inside the `BT` block, SVG `fill` per `tspan`, `CanvasTextLayout.SetColor` on the raster — the live editor needed none, the precondition put the colour back in the document. **`PdfVectorText` did NOT have to change shape**: `PdfVectorTextRun.Colour` from `590d98e` is the second colour's home and the line's `Color` became the fallback, so nothing was traded. **The finding that shaped the row (§43.2): all 106 stored notes in the library carry an explicit run colour nobody picked** — 68 `#FAF9F5`, 38 `#FFFFFF`, none with `\cf0`, none with a `TextColor` — so honouring run colour naively would have frozen every note in the ink of the page it was typed on and made all of them invisible on a page later turned white. `IsMachineInk` folds those back to the box's answer; the default path is proved **byte for byte** unchanged. `TextColourRoundTrip` 16 → **38 checks**, five negative controls. **The unseen link is now SEEN, and it FAILS** (run 21, §44). Two launches against `vp20data`, identical to the pixel. **The row that mattered PASSES:** the `#FFFFFF` box in the shape 38 real notes carry renders `#141413` — 1778 glyph pixels, dark on `#FCFCFC`, readable. `IsMachineInk` is protecting the library and the 38 notes are not invisible. The `#141413` control did not move (802 px). **But neither chosen colour survives:** box E's `#008000` and box M's `#C2185B`/`#1B7F3B` both render `#141413`, and a 155-frame burst from 0.6 s after window creation finds **zero pixels of either colour in any frame** — the colour never reaches the screen at all, so the veil is not undoing it. Worse, the destruction is **still saved**: within ~5 s the app rewrote the model with all four boxes on `\colortbl ;\red20\green20\blue19;`, and all four seeded colours return 0 matches. §43.5's open question is answered **NO**. `RunColoursLost` is harness-proven, so the fault is in the wiring or the timing, not the predicate — §44.3 names the two candidates left and the cheapest probe. **DONE, FIXED** (run 22, §46) — **and the named suspect was wrong.** The `coloursRestored` latch never got the chance to do harm: the probe shows `live` is **already flattened** at the first and only `Loaded` while `builtFrom` still holds the colour, and `RunColoursLost` returned false anyway — so candidate 1's ordering does not hold and candidate 2 never arises, because `SetText` was never reached. The refusal that actually fired is the **character-equality** guard. `GetText(FormatRtf)` on a live `RichEditBox` returns the built-from document **plus one trailing paragraph break** (`want=14 have=15`, `"EEEEEEEEEEEE\n\0"` vs `"EEEEEEEEEEEE\n\n\0"`), so the guard written to mean *"the user typed, do not overwrite a keystroke"* fired on a newline the control itself added — on every chosen-colour box, on every load. Fixed by `TrimTrailingBreaks`, applied to **both** sides before the comparison, dropping trailing `\n`/`\r`/`\0` from the text and its parallel colour list together; symmetric, so it only equalises the tail and a real keystroke is still refused. **On screen**, against a *copy* of `vp20data`: box E **1264 px of `#008000`**, box M **2730 px `#C2185B` + 2363 px `#1B7F3B`**, and `FlushTexts` now saves those colours instead of the collapse — §43.5's question, answered NO in §44.2, is now **YES**. **The half that protects the library is untouched:** box W's stored `#FFFFFF` still folds to **1770 px of `#141413`** (run 21 measured 1778 for the same box) because it refuses one guard earlier, at `HasChosenRunColour`, which never sees the trailing newline. All 38 `TextColourRoundTrip` checks and its five negative controls still pass — the harness builds its own RTF and has never fed the predicate a string from a live `RichEditBox`, which is the only place that extra paragraph comes from. | four emitters + a per-run brush on `CanvasTextLayout` |

## Wave 4 — durability, cheap and worth doing

| # | item | done means |
|---|---|---|
| 4.1 | **`File.Replace(tmp, path, null)`** — **DONE, FIXED**: the only such call was oplog compaction's swap in `SyncLog.cs`; it now passes `path + ".bak"`, matching `LibraryStore.PromoteTemp`'s existing pattern for library.json/settings.json/trash.json. The fixed name is overwritten every compaction (no accumulation) and its `.bak` suffix sits outside the `oplog.*.jsonl` glob `MergeForeign` scans, so it can never be read back as a peer's log. Code-only; not verified on screen. | a backup is taken, or the call is justified in writing |
| 4.2 | **Thumbnail pruning** — **DONE, FIXED**: `ThumbnailCache.Forget(pageId)` deletes every cached PNG variant for one page by id, called from `LibraryStore.DeleteNotebook/DeleteSection/DeletePage` (the pages the caller just spliced out of the live tree, not a rescan) and from `SyncLog`'s foreign-delete merge. No reconciliation sweep against a page-id list, so nothing can be reaped on a stale read. Code-only; not verified on screen. | deleting a page reclaims its thumbnail |
| 4.3 | **`_skipNextRightTap` can stay armed** — **DONE, FIXED**: bound with a 500ms wall-clock window (`Environment.TickCount64`, the same primitive `PenRepairDots` already uses nearby) checked in `OnRightTapped`; both arm sites now go through one `ArmSkipNextRightTap()`. Pointer-id bound rejected — `RightTappedRoutedEventArgs` carries no `PointerId` to compare against, and by the time it fires `_activePointer` is already cleared. Reset-on-gesture-end rejected — one arm site fires FROM inside a gesture-end handler itself. Code-only; not verified on screen. | the flag cannot outlive the gesture that set it |

## Wave 5 — harder, and honest about it

| # | item | done means |
|---|---|---|
| 5.1 | **Renderer failure two keystrokes away** — **DONE, FIXED** (§37): the undo/redo flash and the shape settle pulse both called `_canvas.Invalidate()` from INSIDE `DrawRegion`/`DrawShape`, i.e. with the `CanvasVirtualControl` drawing session still open — an animation repainting itself from within its own `BeginDraw`. `_flashRect` is written only by `FlashAction`, called only from `Undo()`/`Redo()`, which is exactly why it reproduces from Ctrl+Z and not from idle rendering. Both now set `_animRepaint`, consumed once per pass after every session is closed, via `DispatcherQueue.TryEnqueue` (§27's own precedent). Device loss ruled out by the shipped log itself — `IsDeviceLost` is already recorded and no line carries `DEVICE-LOST`; use-after-dispose ruled out by the exception type (a disposed Win2D wrapper raises `ObjectDisposedException`, not `COMException`). Code-only; not verified on screen. | the cause is identified. Logging and retry bounding already shipped; this is the fault itself |
| 5.2 | **Startup does not maximise** — **PARTIAL** (§38): suspect two (**saved bounds reapplied over the maximise**) is **ruled out by reading** — `MoveAndResize` occurs exactly once in `MainWindow.xaml.cs`, *before* both `Maximize()` calls, and nothing anywhere re-applies placement afterwards. Suspect one (**presenter not `OverlappedPresenter`**) is unlikely — the identical guard 20 lines earlier is what removes the system caption and demonstrably takes — but it was **unfalsifiable**: a guarded `if` plus an empty `catch` left no trace either way, which is why this sat unexplained. The remaining mechanism is an **ordering fact**: `App.OnLaunched` runs the whole constructor, where both `Maximize()` calls live, and only *then* calls `Activate()` — so every startup maximise is asserted on a window that has never been shown, and the show that follows can reset it. Fix keeps the constructor attempt (no flash if it holds), re-asserts once on first activation, and logs the presenter's real type/kind to `crash.log` when the guard or the call fails. **PARTIAL because “it maximises” is an on-screen fact and this session did not run the app** — one launch settles it, and §38.5 says what each outcome would mean. | it maximises, and the reason it did not is written down |
| 5.3 | **SyncLog replay** — **DONE, FIXED** (`d271f6e`, §42): the tear window was `File.WriteAllText`, which TRUNCATES before writing. `WriteStateAtomic` writes to .tmp, flushes the encoder into the stream and then `Flush(true)` to physical disk, then `File.Replace` with a .bak — §34’s pattern. .tmp/.bak sit outside the `oplog.*.jsonl` glob so neither can be read back as a peer log. `SyncLog` is the SOLE writer, so this CLOSES the window rather than narrowing it. Not verified on screen; the failure needs a crash at a specific instant. Original: — non-atomic cursor/device-id writes; a torn cursor can resurrect erased strokes | the write is atomic. High severity, low likelihood, needs care |

## Wave 6 — input: the preference that was written and never read

| # | item | done means |
|---|---|---|
| 6.1 | **`HandDrawMode` never restored from `FingerAction`** — **DONE, FIXED** (run 22, §45): `Surface.HandDrawMode` had exactly one writer — `TouchDraw_Click` — so on every launch it was `false` no matter what the user had chosen, while Settings built its Finger Action row from the **persisted** `Library.FingerAction` and drew "Use Active Tool" as selected. The preference was written on every change and applied on no load; that is the whole of the long-standing "a mouse cannot draw in Quill" note, and the engine was blameless. Restored at startup beside the other `Surface.*` restores (`MainWindow.xaml.cs:601`), setting the toggle and the surface together so the "Touch draw" row and the Finger Action strip agree from the first frame. **On screen:** a plain mouse drag lays ink with no setting touched (4 of 4 sampled pixels `#FAF9F5` → ink, a real tapered stroke); the setting round-trips through a restart in **both** directions (`DoNothing` → 0 of 4, `UseActiveTool` → 4 of 4); and §16.10/§17.7 both hold — a 5.83 px press-and-release still selects a stroke, a 30 px one does not. | a plain mouse drag lays ink with no setting touched, and the setting survives a restart |

## Wave 7 — the two rulings, and the first honest look at oil paint

| # | item | done means |
|---|---|---|
| 7.1 | **Mouse Mode row inert while Touch draw is on** — **DONE, FIXED** (run 23, §16.3): the ruling came back *disable the row with a reason, copying the Finger Action row's shown-but-disabled pattern*. Premise re-checked rather than taken on trust: `HandleMousePress` has exactly **one** call site (`InkSurface.cs:1452`), inside `tool == ToolType.Pen && !isPen && !HandDrawMode`, so with Touch draw on none of the four modes dispatches. The row is now `Circle(enabled:false)` + an early `return` in the tap + a per-cell tooltip naming the reason, **and the reason is repeated in the caption** — greying says a control is off and never why, and a tooltip needs a hover a touch reader cannot make. Both writers of TouchDraw (the toggle and the Finger Action strip) now also rebuild `"Keyboard & Mouse"`; they are separate sections on one tab and rebuilding only `"Touch Input"` left the row stale until the panel was reopened. **On screen:** greyed with the reason visible at Touch draw on, fully live with the original caption the instant it is turned off; darkest pixel per circle lifts **+59 to +117** when disabled, the selected one included. | the row is greyed with a visible reason, and comes back live when Touch draw is off |
| 7.2 | **Touch draw caption contradicts the shipped default** — **DONE, FIXED** (run 23): the ruling was *keep the mouse drawing by default, rewrite the caption*. `Library.FingerAction` ships `"UseActiveTool"` (`NoteModels.cs:863`) and `MainWindow.xaml.cs:612` applies it on load, so the switch is **on** out of the box — confirmed on screen, the top-bar hand glyph is lit on a fresh library. Caption now reads *"On is the default — a finger or a mouse drag marks the page with the active tool. Turn it off for pen-first: your palm and your fingers move the page, and only the pen leaves ink."*, matching the register of the surrounding captions. | the caption describes the default the app actually ships |
| 7.3 | **Oil paint: a stroke never scheduled its own write** — **DONE, FIXED** (run 23, §47). **The standing lead was a search in the wrong place**: `PaintTileStore` writes to `%LOCALAPPDATA%\Quill\paint\{sha256(LibraryStore.Dir)[..16]}\{pageId:N}\`, deliberately and documented (`PaintStore.cs:155-158`), so "no `.qtile` under `scratchpad/`" across five scratch folders was the designed outcome, not evidence — tiles from earlier runs were in `%LOCALAPPDATA%` the whole time. The real defect: `OilBrush.CommitScratch` bypasses the `PaintWorld`/`ForEachTile` choke point and ends on `tile.InvalidateLit()`, which sets `Dirty` but does **not** start the §4.3 debounce or the 30 s heartbeat — only `MarkDirty` does. A stroke was therefore never scheduled for writing; the pixels sat in GPU memory, visible and unsaved, until a page switch or the window closing called `FlushBlocking` by hand. Measured: **no `.qtile` for five minutes**, no crashlog either (nothing ran, so nothing failed), then both tiles within a second of `Closed`. Fixed with the `_store.ScheduleSave()` that `PaintTilesAction.Apply` already makes for undo/redo; after it, tiles land **4.4 s** after the stroke with the app running. Also established first-hand, all on screen: a mouse drag paints (the seeded Oil preset is the active pen), one `Ctrl+Z` removes a stroke with no residue, the eraser cuts a clean gap and takes the impasto with it, paint survives a full restart, impasto is **lit** not flat, and paint tiles hold at **1600% and 10%** with no seams — the first measurement of paint past 8x. **Not fixed, reported (§47.4):** `BeginLoad` can upload a tile before the `CanvasVirtualControl` has a device (`paint.crashlog`: *"does not currently have a CanvasDevice"*) and there is no retry. Harmless here and not reproducible on demand; moving the upload into `CreateResources`/`Draw` is loader design, not a missing call. | a stroke reaches disk on its own schedule, proven with the app still running |

## Not code tasks — flagged, not queued

- **Self-signed MSIX cert** — public distribution needs a real certificate. A purchase, not a fix.
- **PDF import rasterises** — imported text not selectable, 2000-page cap. A feature limit, not a defect.
- **`Nearest`'s tie-break follows wheel order**, reversed by the mirror: 1.11% of sRGB queries name a different but *exactly equidistant* code. Left deliberately — the outline must land on a cell the wheel draws.

## Standing rulings the user still owes

- **COPIC seam** — RV42, RV69, RV99, G40, G82 carry almost all of it. A hand-extension, not a re-sourcing.
- **Pen colour in the pen icon** — 48 of 72 pen/paper pairs under 3:1, worst 1.01:1. Live behind `ToolWheel.PenColourInIcon`; needs an outline or a non-page-derived base.
- **Blueprint / Brown Paper dial seats** — excluded from §29's floor because the gate is on L\* not on ratio. Verified to read as clear discs, but if they ever read as absent that gate is a ruling.
- **§28.4's WinUI hover-wash** on `BottomMenu` — WinUI's own plate, not ours.
- ~~**The Mouse Mode row is inert while Touch draw is on and the Pen tool is up**~~ (run 22, §45.4) — **RULED and SHIPPED**, see 7.1. The ruling was *disable the row with a reason, copying the Finger Action row's shown-but-disabled pattern*.
- ~~**The Touch draw caption contradicts the shipped default**~~ (run 22, §45.5) — **RULED and SHIPPED**, see 7.2. The ruling was *the caption is wrong; keep the mouse drawing by default and rewrite it*.

### Newly owed, raised by run 23

- **"Restore defaults" leaves Touch draw and Finger Action disagreeing** (`SettingsWindow.cs`). The reset writes `lib.FingerAction = "UseActiveTool"` (line 2531) and then `_h.SetTouchDraw(false)` (line 2538) — the two halves 6.1 spent a whole run reconciling, put back out of step by the reset path. After a restore the panel contradicts itself exactly as it did before `c16056c`: the Finger Action strip draws "Use Active Tool" as selected while the Touch draw switch reads off, and 7.1's Mouse Mode row shows **live** — then the next launch reads `FingerAction` back, turns Touch draw on and the row goes dead again. **Not fixed, because the fix is a choice, not a repair:** whether Restore Defaults should hand back the shipped default (touch draw **on**, matching `NoteModels.cs:863`) or a pen-first machine (**off**, matching the old caption) is a product ruling. Found by reading, not on screen.
